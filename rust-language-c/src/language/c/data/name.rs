use haskell_support::*;

use data::ix;
use data::int_map;
use int_map;
use qualified;
use data::int_map;
use as;
use int_map;
use data::generics;

pub fn namesStartingFrom(k: isize) -> Vec<Name> {
    vec![Name(k::::)]
}

pub fn newNameSupply() -> Vec<Name> {
    namesStartingFrom(0)
}

